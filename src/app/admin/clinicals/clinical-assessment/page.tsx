
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function ClinicalAssessmentReportsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Clinical Assessment Reports</CardTitle>
                <CardDescription>This page is for managing the final clinical assessment reports. It will allow administrators to compile all evaluation data into a final report for each student's academic record.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Functionality to be implemented.</p>
            </CardContent>
        </Card>
    );
}
